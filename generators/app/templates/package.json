{
  "name": "<%= projectname %>",
  "version": "0.1.0",
  "description": "",<% if (node) { %>
  "main": "server.js",<% } %>
  "author": "<%= author %>",
  "scripts": {
    "development": "webpack --watch & npm run serve",
    "production": "webpack -d -p",<% if (node) { %>
    "start": "forever --minUptime 1000 --spinSleepTime 1000 .",<% } %>
    "serve": "<% if (!node) { %>python -m SimpleHTTPServer<% } else { %>nodemon .<% } %>"
  },
  "devDependencies": {
    "postcss-cssnext": "^2.1.0",
    "postcss-will-change": "^1.0.0",
    "babel-core": "^5.8.22",
    "babel-eslint": "^4.0.10",
    "babel-loader": "^5.3.2",
    "css-loader": "^0.16.0",
    "eslint": "^1.7.1",
    "eslint-loader": "^1.0.0",
    "extract-text-webpack-plugin": "^0.8.2",<% if (!hbs_server && hbs_client) { %>
    "handlebars": "^4.0.3",<% } %><% if (hbs_client) { %>
    "handlebars-loader": "^1.1.1",<% } %>
    "node-sass": "^3.3.3",
    "array.from": "^0.2.0",
    "postcss-loader": "^0.6.0",
    "sass-loader": "^2.0.1",
    "style-loader": "^0.12.3",<% if (react) { %>
    "react": "^0.14.0",
    "react-dom": "^0.14.0",<% if (react_router) { %>
    "react-router": "^1.0.0-rc3",<% } %>
    "eslint-plugin-react": "^3.5.1",<% } %>
    "webpack": "^1.11.0"
  },<% if (node) { %>
  "engines": {
    "node": "4.x.x",
    "npm": "3.x.x"
  },
  "dependencies": {<% if (hbs_server) { %>
    "handlebars": "^3.0.3",<% } %><% if (heroku) { %>
    "forever": "^0.15.1",<% } %>
    "hapi": "^10.0.1",
    "inert": "^3.0.1",<% if (hbs_server) { %>
    "vision": "^3.0.0",<% } %><% if (mongoose) { %>
    "hapi-mongoose-api": "^0.1.0",<% } %>
    "dotenv": "^1.2.0",
    "lodash": "^3.10.1"<% if (hbs_server) { %>,
    "joi": "^6.7.1",
    "yar": "^4.2.0"<% } %>
  },<% } %>
  "license": "MIT"
}
