{
  "name": "<%= projectname %>",
  "version": "0.1.0",
  "description": "",<% if (node) { %>
  "main": "server.js",<% } %>
  "author": "<%= author %>",
  "scripts": {
    "development": "gulp & webpack --watch<% if (node) { %> & nodemon .<% } %>",
    "production": "webpack -d -p<% if (node) { %> && npm start<% } %>",<% if (node) { %>
    "start": "forever --minUptime 1000 --spinSleepTime 1000 .",<% } %>
    "test": ""
  },
  "devDependencies": {
    "autoprefixer-core": "^5.2.1",
    "babel-core": "^5.8.22",
    "babel-eslint": "^4.0.10",
    "babel-loader": "^5.3.2",
    "css-loader": "^0.16.0",
    "eslint": "^1.1.0",
    "eslint-loader": "^1.0.0",
    "extract-text-webpack-plugin": "^0.8.2",
    "gulp": "^3.9.0",
    "gulp-cached": "^1.1.0",
    "gulp-eslint": "^1.0.0",
    "handlebars": "^3.0.3",
    "handlebars-loader": "^1.1.1",
    "node-sass": "^3.3.3",
    "postcss-loader": "^0.5.1",
    "sass-loader": "^2.0.1",
    "style-loader": "^0.12.3",
    "webpack": "^1.11.0"
  },<% if (node) { %>
  "dependencies": {
    "hapi": "^10.0.1",
    "inert": "^3.0.1",
    "lodash": "^3.10.1"
  },<% } %>
  "license": "MIT"
}
